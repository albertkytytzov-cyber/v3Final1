{$PUBLIC_HOST} {
  encode zstd gzip

  @health path /health
  header @health Content-Type application/json
  respond @health "{\"status\":\"ok\",\"service\":\"reverse-proxy\"}" 200

  handle /api/v1/* {
    reverse_proxy api:4000
  }

  handle {
    reverse_proxy web:3000
  }
}
